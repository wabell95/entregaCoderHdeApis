const ItemCount = (onAdd) => {

    const [count, setCount] = uCeState(second)

    const onAdd =() => {}

  return (
    <div>
        <button>+</button>

        <button>-</button>

        <button onClick={onAdd}>agregar al carrito</button>
    </div>
  )
}
export default ItemCount